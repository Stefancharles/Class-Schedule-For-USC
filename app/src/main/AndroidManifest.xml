<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lookup">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/timetable_114px"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".TimeTable.tabledetail"
            android:label="查看课程"
            android:theme="@style/Theme.AppCompat.Light.Dialog">
        </activity>
        <activity android:name=".StuAcademicScore.GetList" />
        <activity android:name=".classroom.roommanage" />
        <activity android:name=".aboutAuthor" />
        <activity android:name=".GradeManage" />
        <activity android:name=".AutoEvaluation" />
        <activity
            android:name=".AddCourseActivity"
            android:label="新建课程"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity android:name=".StudentTimeTable" />
        <activity android:name=".Menu" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>